{% extends 'base.html.twig' %}

{% block title %}DetailsRealisation index{% endblock %}

{% block body %}
    <h1>DetailsRealisation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Chemin_image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for details_realisation in details_realisations %}
            <tr>
                <td>{{ details_realisation.id }}</td>
                <td>{{ details_realisation.cheminImage }}</td>
                <td>
                    <a href="{{ path('app_details_realisation_show', {'id': details_realisation.id}) }}">show</a>
                    <a href="{{ path('app_details_realisation_edit', {'id': details_realisation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_details_realisation_new') }}">Create new</a>
{% endblock %}
