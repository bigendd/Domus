<form method="post" action="{{ path('app_details_realisation_delete', {'id': details_realisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_realisation.id) }}">
    <button class="btn">Delete</button>
</form>
